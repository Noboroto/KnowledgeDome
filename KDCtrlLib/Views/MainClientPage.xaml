<Page
	x:Class="KDCtrlLib.Views.MainClientPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
	Background="{DynamicResource MaterialDesignPaper}"
	TextElement.FontWeight="Medium"
	TextElement.FontSize="14"
	FontFamily="{materialDesign:MaterialDesignFont}"
	DataContext="{Binding Main, Source={StaticResource Locator}}"
	xmlns:control="clr-namespace:KDCtrlLib.KDControls">
	<Page.Resources>
		<ResourceDictionary
			Source="pack://application:,,,/KDCtrlLib;component/Views/GlobalResource.xaml" />
	</Page.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition
				Height="auto" />
			<RowDefinition
				Height="*" />
		</Grid.RowDefinitions>
		<StackPanel				
			Grid.Row="0"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Orientation="Horizontal"
			Margin="25">
			<TextBlock
				HorizontalAlignment="Center"
				Text="{Binding SelectedIP, StringFormat=IP của bạn: {0}}"
				FontSize="30" />
			<TextBlock
				Margin="50 0 0 0"
				VerticalAlignment="Center"
				Text="{Binding CurrentMatch.Name, Mode=OneWay,UpdateSourceTrigger=PropertyChanged, StringFormat=Trận được chọn: {0}}"
				FontSize="30" />
		</StackPanel>
		<ScrollViewer
			VerticalScrollBarVisibility="Auto"
			HorizontalScrollBarVisibility="Disabled"
			VerticalAlignment="Center"
			HorizontalAlignment="Center"
			Grid.Row="1">
			<ListBox
				IsHitTestVisible="False"
				ItemsSource="{Binding CurrentMatch.Players, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
				<ListBox.ItemContainerStyle>
					<Style
						TargetType="ListBoxItem">
						<Setter
							Property="Template">
							<Setter.Value>
								<ControlTemplate
									TargetType="ListBoxItem">
									<control:PlayerMainClientView
										Margin="20" />
								</ControlTemplate>
							</Setter.Value>
						</Setter>
					</Style>
				</ListBox.ItemContainerStyle>
				<ListBox.ItemsPanel>
					<ItemsPanelTemplate>
						<WrapPanel
							Orientation="Horizontal" />
					</ItemsPanelTemplate>
				</ListBox.ItemsPanel>
			</ListBox>
		</ScrollViewer>
	</Grid>
</Page>
