<Page
	x:Class="KDCtrlLib.Views.ServerView.MainServerPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:materialDesignConverters="clr-namespace:MaterialDesignThemes.Wpf.Converters;assembly=MaterialDesignThemes.Wpf"
	xmlns:cmd="http://www.galasoft.ch/mvvmlight"
	xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
	Background="{DynamicResource MaterialDesignPaper}"
	TextElement.FontWeight="Medium"
	TextElement.FontSize="14"
	FontFamily="{materialDesign:MaterialDesignFont}"
	DataContext="{Binding Main, Source={StaticResource Locator}}"
	xmlns:control="clr-namespace:KDCtrlLib.KDControls">
	<Page.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary
					Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
				<ResourceDictionary
					Source="pack://application:,,,/KDCtrlLib;component/Views/GlobalResource.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Page.Resources>
	<Grid
		HorizontalAlignment="Stretch"
		VerticalAlignment="Stretch"
		>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="auto" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition
				Height="auto" />
			<RowDefinition
				Height="auto" />
			<RowDefinition
				Height="*" />
		</Grid.RowDefinitions>
		<StackPanel
			Margin="25"
			Grid.Row="0"
			Grid.Column="0"
			Grid.ColumnSpan="2"
			Orientation="Horizontal"
			VerticalAlignment="Center"
			HorizontalAlignment="Center">
			<TextBlock
				VerticalAlignment="Center"
				FontSize="30">
				IP đã chọn:
			</TextBlock>
			<ComboBox
				Margin="10 10 50 10"
				VerticalAlignment="Center"
				FontSize="30"
				SelectedItem="{Binding SelectedIP, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
				ItemsSource="{Binding IPs}"
				SelectedIndex="0"
				MinWidth="200" />
			<TextBlock
				Margin="50 0 0 0"
				Text="Trận đã chọn: "
				VerticalAlignment="Center"
				FontSize="30" />
			<ComboBox
				ItemsSource="{Binding matches}"
				SelectedItem="{Binding CurrentMatch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
				DisplayMemberPath="Name"
				Margin="10 10 50 10"
				VerticalAlignment="Center"
				FontSize="30"
				SelectedIndex="{Binding SelectedMatchIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
				MinWidth="200" />
		</StackPanel>
		<TextBlock
			Margin="10"
			FontSize="50"
			Grid.Row="1"
			Width="auto"
			Height="auto"
			Text="  HÃY CHỌN NGƯỜI CHƠI  "
			HorizontalAlignment="Center">
			<TextBlock.Style>
				<Style
					TargetType="TextBlock"
					BasedOn="{StaticResource MaterialDesignHeadline1TextBlock}"
					>
					<Style.Triggers>
						<DataTrigger
							Binding="{Binding Path=MainFrame.IsPending, Source={StaticResource Locator}}"
							Value="True">
							<Setter
								Property="Visibility"
								Value="Visible" />
						</DataTrigger>
						<DataTrigger
							Binding="{Binding Path=MainFrame.IsPending, Source={StaticResource Locator}}"
							Value="False">
							<Setter
								Property="Visibility"
								Value="Hidden" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</TextBlock.Style>
		</TextBlock>
		<ScrollViewer
			VerticalScrollBarVisibility="Auto"
			HorizontalScrollBarVisibility="Disabled"
			Grid.Row="2"
			Grid.Column="0"
			>
			<Grid
				VerticalAlignment="Center"
				HorizontalAlignment="Center"
				>
				<Grid.RowDefinitions>
					<RowDefinition />
					<RowDefinition Height="auto" />
				</Grid.RowDefinitions>
				<ListBox
					ItemsSource="{Binding CurrentMatch.Players}">
					<i:Interaction.Triggers>
						<i:EventTrigger
							EventName="SelectionChanged">
							<cmd:EventToCommand
								PassEventArgsToCommand="True"
								Command="{Binding SelectPlayerCmd}" />
						</i:EventTrigger>
					</i:Interaction.Triggers>
					<ListBox.Style>
						<Style
							BasedOn="{StaticResource MaterialDesignListBox}"
							TargetType="ListBox">
							<Style.Triggers>
								<DataTrigger
									Binding="{Binding Path=MainFrame.IsExtra, Source={StaticResource Locator}}"
									Value="False"
									>
									<Setter
										Property="SelectionMode"
										Value="Single" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</ListBox.Style>
					<ListBox.ItemContainerStyle>
						<Style
							BasedOn="{StaticResource MaterialDesignListBoxItem}"
							TargetType="ListBoxItem">
							<Setter
								Property="materialDesign:ListBoxItemAssist.ShowSelection"
								Value="{Binding Path=MainFrame.IsPending, Source={StaticResource Locator}}" />
						</Style>
					</ListBox.ItemContainerStyle>
					<ListBox.ItemTemplate>
						<DataTemplate>
							<control:PlayerMainServerView
								Margin="20" />
						</DataTemplate>
					</ListBox.ItemTemplate>
					<ListBox.ItemsPanel>
						<ItemsPanelTemplate>
							<WrapPanel
								VerticalAlignment="Center"
								HorizontalAlignment="Center"
								Orientation="Horizontal" />
						</ItemsPanelTemplate>
					</ListBox.ItemsPanel>
				</ListBox>
				<Button
					Content="Vào câu hỏi phụ"
					Margin="10"
					Command="{Binding Path=MainFrame.StartExtraCmd, Source={StaticResource Locator}}">
					<Button.Style>
						<Style
							TargetType="Button"
							BasedOn="{StaticResource MaterialDesignRaisedButton}">
							<Style.Triggers>
								<DataTrigger
									Binding="{Binding Path=MainFrame.IsExtra, Source={StaticResource Locator}}"
									Value="False">
									<Setter
										Property="Visibility"
										Value="Hidden" />
								</DataTrigger>
								<DataTrigger
									Binding="{Binding Path=MainFrame.IsExtra, Source={StaticResource Locator}}"
									Value="True">
									<Setter
										Property="Visibility"
										Value="Visible" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</Button.Style>
				</Button>
			</Grid>
		</ScrollViewer>
	</Grid>
</Page>
