<Page
	x:Class="KDCtrlLib.Views.MainServerFramePage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
	Background="{DynamicResource MaterialDesignPaper}"
	TextElement.FontWeight="Medium"
	TextElement.FontSize="14"
	FontFamily="{materialDesign:MaterialDesignFont}"
	DataContext="{Binding MainServerFrame, Source={StaticResource Locator}}">
	<Page.Resources>
		<ResourceDictionary
			Source="GlobalResource.xaml" />
	</Page.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition
				Width="3*" />
			<ColumnDefinition
				Width="5" />
			<ColumnDefinition
				Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition
				Height="*" />
			<RowDefinition
				Height="5" />
			<RowDefinition
				Height="*" />
		</Grid.RowDefinitions>
		<Frame
			x:Name="MainFrame"
			Grid.Column="0"
			Grid.Row="0"
			Grid.RowSpan="3" />
		<GridSplitter
			Grid.Column="1"
			Grid.RowSpan="3"
			Width="5"
			HorizontalAlignment="Stretch" />
		<GridSplitter
			Height="5"
			Grid.Column="2"
			Grid.Row="1"
			HorizontalAlignment="Stretch" />
		<Grid
			Margin="10"
			Grid.Column="2"
			Grid.Row="0"
			HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch">
			<Grid.RowDefinitions>
				<RowDefinition />
				<RowDefinition
					Height="auto" />
			</Grid.RowDefinitions>
			<ListBox
				IsTextSearchEnabled="False"
				FontSize="18"
				DockPanel.Dock="Top"
				FontFamily="Segoe UI"
				x:Name="Chatting"
				VirtualizingStackPanel.IsVirtualizing="True"
				Foreground="White"
				Grid.Column="2"
				Grid.Row="0"
				HorizontalAlignment="Stretch"
				VerticalAlignment="Stretch"
				SelectionMode="Single">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<StackPanel
							Orientation="Horizontal">
							<TextBlock
								Foreground="{Binding ForegroundColor}"
								Text="{Binding Info}"
								/>
							<TextBlock 
								Text="{Binding Message}"
								/>
						</StackPanel>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
			<TextBox
				x:Name="SendMessage"
				KeyDown="TextBox_KeyDown"
				Margin="0 10 0 10"
				Grid.Row="1"
				TextWrapping="Wrap"
				HorizontalAlignment="Stretch"
				HorizontalContentAlignment="Left"
				AcceptsReturn="False"
				VerticalScrollBarVisibility="Auto"
				SpellCheck.IsEnabled="False"
				materialDesign:HintAssist.Hint="Gửi đến MC"
				Height="auto" />
		</Grid>
		<ListBox
			Grid.Column="2"
			Grid.Row="2"
			FontFamily="Segoe UI"
			IsTextSearchEnabled="False"
			FontSize="18"
			x:Name="Logging"
			VirtualizingStackPanel.IsVirtualizing="True"
			Foreground="White"
			HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch"
			SelectionMode="Single">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel
						Orientation="Horizontal">
						<TextBlock
							Text="{Binding Info}" />
						<TextBlock
							Text="{Binding Message}" />
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</Page>
